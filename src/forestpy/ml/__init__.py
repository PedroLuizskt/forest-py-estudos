"""
Machine Learning aplicado à floresta — clássico e deep learning.

Submódulos:
    preprocessing  — Pipelines reproduzíveis sklearn-compatible
    classical      — Wrappers para LinReg, RandomForest, XGBoost, SVR
    mlp            — Redes neurais tabulares (PyTorch)
    cnn            — Redes convolucionais (PyTorch + torchvision)
    metrics        — RMSE, MAE, MAPE, R², bias, IoU, Dice
    evaluation     — Cross-validation, bootstrap, holdout
"""
